%header.masthead
  .container
    .row.page_header.admin
      .col-md-6
        %h1 Klubbar

    .row.spacing
      = render :partial => 'admins/partials/menu'

      .col-md-10
        %table.table.table-hover
          %thead
            %th Namn
            %th
          %tbody
            - @clubs.each do |u|
              %tr
                %td= u.name
                %td
                  = link_to "", edit_club_path(u), class: "glyphicon glyphicon-pencil"
